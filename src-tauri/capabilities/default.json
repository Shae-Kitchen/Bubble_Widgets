{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "timer", "braindump", "dashboard"],
  "permissions": ["core:default", "opener:default", "fs:default"]
}
